<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="net.ddns.gingerpi.chessboardnet">

    <uses-permission android:name="android.permission.INTERNET" />

    <permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="false"
        android:largeHeap="true"
        android:icon="@drawable/doodle_white_pawn"
        android:label="@string/app_name"
        android:roundIcon="@drawable/doodle_white_pawn"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Login" android:launchMode="singleInstance"/>
        <activity android:name=".ChessPlayer" android:launchMode="singleInstance" android:screenOrientation="landscape"></activity>
    </application>

</manifest>